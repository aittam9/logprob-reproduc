# Direct dependencies only -- transitive deps (torch's CUDA wheels, tokenizers,
# huggingface-hub, etc.) are resolved automatically by pip and are left
# unpinned here so they can match whatever platform (Linux/CUDA, macOS, CPU)
# this is installed on. Install PyTorch separately first if you need a
# specific CUDA build: https://pytorch.org/get-started/locally/
nnsight==0.5.12
torch==2.12.1
transformers==5.12.1
pandas==3.0.3
tqdm==4.68.3
